export type Review = {
  id: number;
  img: string;
  name: string;
  role: string;
  title: string;
  content: string;
  bgColor: string;
  titleColor: string;
  contentColor: string;
  gridArea?: string;
  bgPattern?: string;
};
